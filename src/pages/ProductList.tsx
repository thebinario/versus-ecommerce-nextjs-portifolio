import React from 'react';

const ProductList = () => {
    return (
        <div>
            <h1>Produtos</h1>
            {/* Listagem de produtos */}
        </div>
    );
};

export default ProductList;
