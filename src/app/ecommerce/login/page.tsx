'use client';
import React from 'react';

const Page = () => {
    return (
        <div>
            <h1>Login</h1>
            {/* Formulário de login */}
        </div>
    );
};

export default Page;
